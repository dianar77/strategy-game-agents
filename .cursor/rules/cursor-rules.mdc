# General

- Update Architecture and major code changes in Architecture.md in the respective project (i.e. frontend, backend, etc)
- Don't change software architecture unless instructed to do so


# Database
- Use the file "database model.sql" as a refence for the database structure
- Don't modify the database structure unless instructed to do so
